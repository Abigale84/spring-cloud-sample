spring:
  cloud:
    config:
      name: eurekaclient
      label: master
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/